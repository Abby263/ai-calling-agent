[project]
name = "voice-concierge-livekit-agent"
version = "0.1.0"
description = "LiveKit Agents worker for Voice Concierge outbound calls"
requires-python = ">=3.11,<3.13"
dependencies = [
  "httpx>=0.27.0",
  "livekit-agents[openai]~=1.4",
  "livekit-plugins-noise-cancellation~=0.2",
  "python-dotenv>=1.0.1",
]

[tool.uv]
package = false
